apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: application
  labels:
    training: block3
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: application
                port:
                  number: 8080
